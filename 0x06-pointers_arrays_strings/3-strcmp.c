#include "main.h"
#include <ctype.h>
#include <string.h>
/**
 * _strcmp - compare strings
 * @s1: string to be compared
 * @s2: second string to be compared
 * Description: function that compares two strings and determine if same
 * Return: nada
 **/

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}

