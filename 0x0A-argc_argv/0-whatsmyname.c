#include "main.h"
#include <stdio.h>

/**
 * main -  main function to return standard C inputs.
 *@argc: size of input array.
 *@argv: array of input parameters
 * Return: nada
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
